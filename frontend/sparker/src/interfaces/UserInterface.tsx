interface UserType {
    id: number;
    firstName: string;
    lastName: string;
    bio: string;
    isAdmin: boolean;
    isMaster: boolean;
    registrationAt: string;
}

export default UserType;

